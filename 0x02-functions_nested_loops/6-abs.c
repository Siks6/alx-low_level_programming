#include "main.h"

/**
 * _abs - check main
 * @r: An integer input
 * description: This function returns absolute value of anumber
 * Return: Absolute value of number r
 */
int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (r * -1);
}
